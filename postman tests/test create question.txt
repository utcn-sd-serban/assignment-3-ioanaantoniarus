pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});


pm.test("Content-Type is present", function () {
    pm.response.to.have.header("Content-Type");
});

pm.test("Response time is less than 200ms", function () {
    pm.expect(pm.response.responseTime).to.be.below(200);
});

let jsonData = pm.response.json();

pm.test("Test title is second question", function () {
    pm.expect(jsonData.title).to.eql("second question");
});

pm.test("Test username is ioana33", function () {
    pm.expect(jsonData.username).to.eql("ioana33");
});

pm.test("Test text is second text", function () {
    pm.expect(jsonData.text).to.eql("second text");
});